interface MatchScoreProps {
    score: number;
    size?: 'sm' | 'md' | 'lg';
    showText?: boolean;
}
export declare const MatchScore: ({ score, size, showText }: MatchScoreProps) => import("react/jsx-runtime").JSX.Element;
export {};
